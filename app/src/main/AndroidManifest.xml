<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mp2021_2_9">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MP202129" >

        <activity android:name=".MainActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".LoginActivity" android:label="Login page"/>
        <activity android:name=".joinActivity"  android:label="Join page"/>
        <activity android:name=".DetailPromotionActivity" android:label="Promotion Detailed page"/>
        <activity android:name=".DetailGoodsActivity" android:label="Goods Detailed Page"/>

    </application>

</manifest>